# Legacy widget runtime (duplicated)

This directory contains the legacy widget runtime that used to live under `/widget/src`.
It is **not** used by the production build. The production widget is built from `/src`.

Do not edit this code for new features. If you need to change widget behavior, update `/src` instead.
