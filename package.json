{
  "name": "valki-talki-widget",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "build": "node build/esbuild.mjs && npm run check:widget",
    "check:widget": "node build/check-widget-placeholders.mjs",
    "ci": "npm ci",
    "test:e2e": "npx playwright test",
    "test:embed": "npx playwright test tests/embed-compat.spec.ts"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.1",
    "esbuild": "file:build/esbuild-shim"
  }
}
